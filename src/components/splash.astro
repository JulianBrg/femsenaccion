---
import { Image } from "astro:assets";
import womanFace from "~/assets/woman-face.png";

const widths = [450, 800];
const sizes = "(min-width: 640px) 42vw, 67vw";
---

<section class=`relative h-full bg-[#4f245a]`>
  <!-- <Starfield /> -->

  <div class="relative grid h-full place-items-center sm:grid-cols-2">
    <div
      class="flex flex-col items-center gap-2 self-end sm:gap-4 sm:self-auto sm:justify-self-end"
    >
      <!-- <Icon name="logomark" class="size-20 text-white md:size-15" /> -->
      <h1
        class="inline-block bg-gradient-to-r from-white via-pink-100 to-white bg-clip-text text-center font-extrabold tracking-tighter text-transparent sm:text-xl text-xs"
      >
        Fems en Acción por México
      </h1>
      <h2
        class="inline-block bg-gradient-to-r from-white via-pink-300 to-white bg-clip-text text-center font-extrabold tracking-tighter text-transparent text-4xl"
      >
        Impulsando el liderazgo, empoderamiento,
        <br /> bienestar y desarrollo integral de las mujeres.
      </h2>
    </div>

    <div
      id="astronaut"
      class="w-2/3 max-w-3xl self-start sm:w-10/12 sm:self-auto sm:justify-self-start"
    >
      <Image
        class="h-full w-full object-cover"
        src={womanFace}
        widths={widths}
        sizes={sizes}
        loading="eager"
        alt="A floating astronaut in a space suit"
      />
    </div>
  </div>
</section>

<noscript>
  <style>
    #splash-bg-fallback {
      display: block;
    }
  </style>
</noscript>

<style>
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 10px, 0);
    }
  }

  #astronaut {
    animation: float linear 2.5s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    #astronaut {
      @apply animate-none;
    }

    :global(#starfield) {
      @apply hidden;
    }

    #splash-bg-fallback {
      @apply block;
    }
  }
</style>
